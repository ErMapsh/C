#include<stdio.h>
int main()
{
    FILE *ptr = NULL;
    char string[34];

    //*******reading a file*******
    // ptr = fopen("maintext.txt", "r");
    // fscanf(ptr, "%s", string);
    // printf("%s", string);
    // fclose(ptr);


    //*******writing a file*******
    // char string1[100] = "this is a string1,for writing operation";
    // ptr = fopen("maintext.txt", "w");
    // fprintf(ptr, "%s", string1);
    // fclose(ptr);

    return 0;
}